ReceiptsDetailed.java(商品明细表实体类)中，加了一个商品信息对象
                                         加了一个供货商对象
                                         加了一个单据信息对象
Receipts.java(单据信息实体类)中，加了一个单据类型对象
                               加了一个仓库对象

Receipts.java(单据信息实体类)中，新增了四列
                           应付金额(redAmountPrice)
                           实付金额(redActuallyPrice)
                           优惠金额(redDiscountPrice)
                           操作员(redOperator)
                           【数据库中也增加了】
                   开始时间(startTime)、结束时间(endTime)
                   当前页数(page)、显示条数(limit)
                   模糊查询参数(val)
                   【只在实体类中增加，数据库中不要增加】

commodity(商品表)--》duo1:数量
                ---》duo2:生产日期
                ---》duo3:有效期至

http://localhost:8008/PurchaseReturn.html